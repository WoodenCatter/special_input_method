下面描述的输入法有**两条并行流程**，而不是一条流程

> **第一条：用户看到的扫描交互流程（前端）**
>
> **第二条：系统后台的拼音恢复流程（后台）**
---

# BCI扫描式中文输入法完整工作流程

## 一、设计目标

本输入法是一种面向脑机接口（BCI）的扫描式中文输入系统。

由于BCI无法像传统键盘一样连续输入字符，因此整个输入过程采用"**扫描 + 任务确认**"的交互方式。用户无需点击按钮，只需通过任务"左看""右看""咬牙"产生的脑机信号完成少量控制即可完成中文输入，信号采集部分先流出接口。

系统将传统拼音输入法拆分为多个阶段，通过分层扫描、候选预测、拼音合法性约束以及语言模型预测，尽可能减少用户确认次数，提高BCI输入效率。整个系统主要由两个相互独立但实时协同工作的模块组成：

1. 前端扫描交互模块；
2. 后台拼音恢复与候选生成模块。

前端负责人与系统的交互，后台负责根据当前输入状态实时计算合法拼音、候选汉字及预测结果。

---

# 二、前端扫描交互流程

## （1）系统初始状态

系统启动后，进入一级扫描界面。

屏幕显示八个字母分组，每组对应若干英文字母，分在屏幕两侧，中间有两块区域备用，分别为区域1和区域2。

如下面显示：

```
ABC     区域1       MNO
DEF                 PQRS
GHI     区域2       TUV
JKL                 WXYZ
```

八个字母块分别排列在左右两侧。

系统按照固定顺序自动高亮当前所在侧的扫描块(初始为左侧)。

例如：

```
ABC

↓

DEF

↓

GHI

↓

JKL

↓

ABC……
```

整个扫描持续循环，扫描速度可以由用户自定义。

中间有两块区域，上面的区域同步放大显示当前所扫描块里的字母，同时在左上角或右上角显示左/右字样(高亮的块在左侧则显示“左”)。

下面的区域初始状态分为两种：
①如果是发起对话，可以显示一些预测出的常用语
②如果是回答对话，可获取上下文显示可能的回答
③如果是其他场景请自主规划

---

## （2）选择第一层字母块

当目标拼音所在字母块被高亮时，如果当前高亮块在左侧，用户执行"左看"操作后选中当前高亮块，如果高亮块在右侧则执行“右看才能选中”。

当用户通过左看/右看选中某块后，中间区域下面的块分为两部分，上面一部分显示当前所选字母，下面一部分显示当前所选块所可能产生的汉字(根据使用频率或者现有开源输入法统计)，高亮循环仍继续

例如一级选择"ABC"块。

当 ABC 高亮时：

```
ABC（高亮）
```

用户左看。

区域2的上面一部分显示 a b c

区域2的下面一部分显示 “吧 啊 包 次 吃 棒......”(这是个示例，参考的iPhone九键输入法)

区域2进入伪固定态，暂时不发生改变，两侧高亮循环仍继续

假如只选择一个字母块后预测区(中间区域下面的块)中出现了目标字“棒”，咬牙进入下一层

这里需要注意，用户选择的不是具体字母，而是一个字母块。

用户可以在第一层选择多个字母块，在中间区域下面区域的上面部分显示所选块产生的合法拼音(通过维护一个拼音前缀树来实现O(1)时间复杂度来实现)，在中间区域下面区域的下面部分显示所选块产生的汉字

例如：通过左看选中“ABC”->通过右看转到右侧->目标块高亮后，通过右看选中“TUV”

区域2显示：
```
bu cu
不 部 补 布 粗 醋 促 捕
```
---

## （3）进入第二层
当用户在第一层选中一个或多个块后区域二下半部分出现了目标字/词后，通过咬牙进入第二层。

进入第二层以后，界面两侧仍显示八个字母块，并且不进行高亮循环，用户通过左右看在区域2上半部分进行拼音选择。

例如 在第一层只选择了ABC块，在第二层区域二：

```
a(高亮)  b  c
吧 啊 吖 阿 嗄......
```
（同时区域一放大显示a对应下面的字，b、c以此类推）

此时通过左看或者右看来使a b c高亮并观察候选区是否有自己想要的字

例如 此时左看，则c变成高亮，同时区域2下半部分和区域1显示c对应候选词
```
a  b  c(高亮)
从 才 吃 出 陈 成......
```
当目标组合或字母被高亮后，再次执行咬牙进入汉字选择，汉字在第一页循环高亮，如在a高亮时咬牙，区域二下半部分：

```
a
吧(高亮) 啊 吖 阿 嗄 呵 
腌 锕 哎 爱 按 下一页
```
当目标字高亮后咬牙选择，完成一个字的输入

---

如果进入第二层时选择了多个字母块，则只显示合法组合


## （4）预测词选择

一个汉字输入完成后，系统不会立即返回一级扫描。

而是首先根据上下文预测后续内容。

例如：

用户刚输入：

```
不
```

系统可能预测：

```
不是 不要 不会 不能 不可以
```

预测结果同样在区域二下半部分并采用扫描方式展示。

若目标词高亮时同样通过咬牙确认

整个词即可一次输入完成。

在最后加一个继续的选择，以便若在最后若预测未命中，则退出预测阶段，返回到第一层，继续输入下一个汉字。

整个流程不断循环，加个发送按钮，扫描到发送时用户可通过咬牙发送。

---

## （5）左右扫描切换

笔记中设计了左右区域扫描机制。

系统扫描过程中，左右区域可以相互切换。

用户可以通过"左/右"看切换区域

在左侧时右看可以切换到右侧扫描，在右侧时左看可以切换到左侧扫描
例如：

```
左侧扫描

↓

右看

↓

右侧扫描

↓

左看

↓

返回左侧
```

左右扫描循环进行。

这样能够避免扫描路径过长，提高扫描效率。

---

# 三、后台拼音恢复流程

前端完成扫描的同时，后台持续运行拼音恢复算法。

用户实际上输入的是字母块，而不是具体字母。

因此后台需要不断恢复所有可能的拼音。

整个后台流程如下。

---

## （1）字母块编码

系统首先将26个字母映射为八个字母块。

例如：

```
ABC

DEF

GHI

……

WXYZ
```

用户每完成一次一级选择，后台都会记录对应字母块编号。

例如输入过程得到：

```
64426
```

该编码并不能唯一确定拼音。

---

## （2）恢复所有可能字母

由于一个字母块对应多个字母。

例如：

```
6

↓

m
n
o
```

因此：

```
64426
```

可以恢复出大量字母组合。

例如：

```
mihao

nihao

mihan

……

```

理论上可能存在几十甚至上百种组合。

因此不能直接全部保留。

---

## （3）Trie合法性过滤

后台维护完整拼音前缀树（Trie）。

恢复每增加一个字母后，立即检查是否合法。

例如：

恢复：

```
mh
```

Trie不存在。

立即删除。

恢复：

```
mi
```

Trie存在。

继续保留。

随着输入不断增加。

非法路径不断剪枝。

最终仅剩合法拼音。

例如：

```
nihao

nihan

mihao

……
```

这一步极大降低了后续搜索空间。

---

## （4）拼音切分

恢复出的字符串仍然只是连续字母。

例如：

```
nihao
```

后台进一步尝试所有合法拼音切分。

例如：

```
ni + hao
```

另一种：

```
ni + han
```

非法切分立即删除。

保留所有合法拼音序列。

---

## （5）拼音转汉字

对于每一个拼音序列。

查询拼音词典。

例如：

```
ni

↓

你
尼
拟
……

hao

↓

好
号
毫
……
```

组合得到所有汉字候选。

---

## （6）语言模型排序

最后使用中文语言模型结合上下文进行排序。

综合考虑：

* 当前上下文；
* 词频；
* 常见搭配；
* 语言模型概率。

例如：

```
ni hao
```

最终排序：

```
你好

拟好

泥号

……
```

排序结果返回前端。

前端优先显示概率最高候选。

因此用户通常只需一次确认即可完成输入。

---

# 四、系统整体工作流程

整个输入法实际上由前端扫描和后台解码两个流程并行组成。

```
前端

一级扫描
        │
        ▼
选择字母块
        │
        ▼
第二层选择拼音组合
        │
        ▼
选择候选汉字
        │
        ▼
选择预测词
        │
        ▼
继续下一字
```

与此同时：

```
后台

字母块编码
        │
        ▼
恢复所有可能字母
        │
        ▼
Trie合法性过滤
        │
        ▼
拼音切分
        │
        ▼
拼音转汉字
        │
        ▼
语言模型排序
        │
        ▼
返回候选与预测
```

前端负责人与系统的交互，后台负责实时计算候选结果，两者持续协同工作，共同完成整个BCI扫描式中文输入过程。

整个系统的核心目标是**在保证输入准确性的前提下，尽可能减少用户需要进行的扫描等待和确认次数**。

